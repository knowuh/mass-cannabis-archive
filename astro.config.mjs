import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  output: 'static',
  site: 'https://npaessel.github.io',
  base: '/mass-cannabis-archive', // Adjust if this is the repo name
});
